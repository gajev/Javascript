function Substring(word, first_number, second_number) {
    let finalString = word.substring(first_number, first_number + second_number);
    console.log(finalString)
}

Substring('ASentencetest', 1, 8)
Substring('SkipWord', 4, 7)

